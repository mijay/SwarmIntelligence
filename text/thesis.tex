\documentclass[14pt,a4paper,times,bachelor,
    subf,href,
    draft
]{disser}

\usepackage[
  a4paper, mag=1000, includefoot,
  left=3cm, right=1cm, top=2cm, bottom=2cm, headsep=1cm, footskip=1cm
]{geometry}
\usepackage[T2A]{fontenc}
\usepackage[cp1251]{inputenc}
\usepackage[english,russian]{babel}
\usepackage{tabularx}           % таблицы

% списки:
\usepackage{enumitem}
\AddEnumerateCounter{\asbuk}{\asbuk}{щ}
\setlist[2]{label={\asbuk*}.}

% сокращени€:
\usepackage[intoc, russian]{nomencl}  
\makenomenclature
\renewcommand{\nomlabel}[1]{\hbox to 6cm{#1}} % делаем выравнивание и отступ
\renewcommand{\nomname}{ѕеречень прин€тых в работе сокращений}


\ifpdf\usepackage{epstopdf}\fi

\usepackage[final]{listings}    % вставка листингов кода
\lstloadlanguages{[Sharp]C}     % загрузка используемых €зыков
\lstset{tabsize=2,              % изменить настройки пакета: помен€ть размер табов (4 а не 8)
commentstyle=\itshape,          % мен€ем стиль комментариев на италик
frame=lines,                    % обрамл€ть листинг лини€ми сверху и снизу
framesep=10pt, texcl,           % отступ до кода + комментарии в коде обрабатываютс€ LaTeX
aboveskip=15pt,belowskip=25pt,  % отступы до и после листинга
language=[Sharp]C,
basicstyle=\small}

\usepackage{amssymb}            % доп. матсимволы + <= в русском стиле
\renewcommand{\ge}{\geqslant}   % <= по умолчанию русское
\renewcommand{\le}{\leqslant}   % >= по умолчанию русское

% Ќомера страниц сверху справа
\def\headfont{\small}
\pagestyle{headright}
\chapterpagestyle{headright}

% »спользовать полужирное начертание дл€ векторов
\let\vec=\mathbf

% ¬ключать подсекции в оглавление
\setcounter{tocdepth}{2}
\usepackage{indentfirst}        % отступ у первого абзаца раздела
%\numberwithin{equation}{section}% включать в номера формул номера разделов

%\graphicspath{{fig/}}

\newcommand{\todo}[1]{\marginpar{TODO: #1}}

\begin{document}
\include{title}
\setcounter{page}{2}
\tableofcontents
\include{intro}
\include{first}
\include{second}
\include{concl}

\bibliographystyle{gost705}
\bibliography{thesis}
\end{document}
